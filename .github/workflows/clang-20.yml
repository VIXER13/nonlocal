name: clang

on:
  push:
    branches: [ master, dev ]
    paths-ignore:
      - 'documents/**'
      - '*.md'

jobs:
  20:
    uses: ./.github/workflows/common.yml
    with:
      compiler: clang-20
      deps: clang-20 libomp-20-dev