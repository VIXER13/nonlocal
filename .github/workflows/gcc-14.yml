name: gcc

on:
  push:
    branches: [ master, dev ]
    paths-ignore:
      - 'documents/**'
      - '*.md'

jobs:
  14:
    uses: ./.github/workflows/common.yml
    with:
      compiler: gcc-14
      deps: gcc-14 g++-14